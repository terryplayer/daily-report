# 2026-06-06 · RAG 架构升级日

## 今日完成：混动系统 RAG 全面升级

### 🧠 RAG 记忆系统增强
- **rag_memory.py** 升级：
  - 新增 `index_daily_snapshot()` — 结构化行情快照索引到FAISS
  - 新增 `search_similar_trading_days()` — 按行情特征搜索历史相似日
  - 新增 `calc_historical_similarity()` — 计算历史相似度得分（新模型维度）
  - 新增 `format_rag_context_for_report()` — 为报告管线生成结构化RAG上下文
  - 独立快照索引（13维特征向量：基准涨跌 + 板块涨跌soft-sign + 市场宽度 + RS模拟 + 数据质量）

### 📸 每日快照管线
- 新建 `gen_daily_snapshot.py` — 收盘后自动生成结构化快照并索引
- 支持 `--backfill` 回填历史交易日（8/12天已索引）

### 📊 统一模型升级（第6维度）
- 原：RS(25%) + 动量(20%) + MRD(10%) + 多因子(30%) + 轮动(15%)
- **新：RS(23%) + 动量(18%) + MRD(9%) + 多因子(27%) + 轮动(13%) + 历史相似度(10%)**
- `stock_analysis.py` 的 `get_model_summary()` 和 `format_model_summary_text()` 已更新

### 🌲 随机森林增强（第11维特征）
- RF 特征从 10 维 → **11 维（+历史相似度）**
- `_compute_date_similarity_for_rf()` — 训练时动态计算，防止数据泄露
- **历史相似度特征重要性排第6（0.068）**，超过RS值(0.031)、多因子(0.023)

### 📋 Taskflow 集成
- 三份 taskflows（盘前/午间/收盘）各加入 RAG 检索步骤
- 收盘 taskflow 加入自动快照生成

### 🧪 测试结果
- RAG文本索引: 94 chunks ✅
- RAG快照索引: 8 个交易日 ✅
- 统一模型: 6维度覆盖所有板块 ✅
- 随机森林: 11维度 + RF训练正常 ✅
